let multiVal = ["hey", true, "pw skills", null, false, "there", "pw skills", 20, "ineuron"]
let firstIndex = multiVal.indexOf("pw skills")
let lastIndex = multiVal.lastIndexOf("pw skills")
console.log(`First index: ${firstIndex}, last index: ${lastIndex}`);